{
  "name": "auth0-tenant-reset",
  "version": "1.0.0",
  "description": "An Auth0 tool/extension that can be used to reset a tenant to a known set of artifacts.",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && babel src -d dist",
    "start": "node dist/index.js"
  },
  "repository": "https://github.com/auth0-extensions/auth0-tenant-reset",
  "author": "twistedstream",
  "license": "MIT",
  "dependencies": {
    "auth0": "^2.3.1",
    "colors": "^1.1.2",
    "dotenv": "^2.0.0",
    "request": "^2.74.0"
  },
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-eslint": "^6.1.2",
    "babel-preset-node6": "^11.0.0",
    "eslint": "^3.3.1",
    "eslint-plugin-import": "^1.13.0",
    "rimraf": "^2.5.4"
  }
}
